package com.yunus.ecommerce.mapper;

import org.mapstruct.Mapper;

@Mapper
public interface BasketMapper {

    /*
    @Mapping(target = "basketId",source = "basket.basketId")
    @Mapping(target = "productPrice",source = "basket.productPrice")
    @Mapping(target = "subTotal",source = "basket.subTotal")
    @Mapping(target = "productId",source = "basket.product")
    @Mapping(target = "orderId",source = "basket.order")

    BasketDto basketToBasketDto(Basket basket);

     */
}
